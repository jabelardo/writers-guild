# Writers Guild Server Configuration

server:
  port: 8001
  host: '0.0.0.0'

data:
  # Data directory for stories, characters, and settings
  root: '../data'

security:
  # CORS configuration
  cors:
    origins:
      - 'http://localhost:8001'
      - 'http://127.0.0.1:8001'

  # Future: Add basic auth, CSRF protection, etc.
